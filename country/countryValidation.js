const id = 'Country Id is required.'
const countryName = 'Country name is required.'
const created = ' Country(along with cities) created.'
const deleted = ' Country deleted.'
const updated = ' Country updated'
const notFound = ' Country not found.'
const notCreated = ' Country(along with cities) could not be created.'
const notDeleted = ' Country could not be deleted.'
const notUpdated = ' Country could not be updated'
module.exports = {
  id,
  countryName,
  created,
  deleted,
  updated,
  notCreated,
  notDeleted,
  notUpdated,
  notFound
}
